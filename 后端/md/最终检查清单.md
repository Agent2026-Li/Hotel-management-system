# 最终检查清单

- 本次后端重构未修改前端代码。
- 后端工程位于 `后端/hrms-backend`，文档位于 `后端/docs`。
- 已改为原生 MyBatis，不使用 MyBatis-Plus。
- 已补充 `mapper` 层，简单 SQL 使用注解，复杂 SQL 使用 XML。
- 已移除 `DemoDataStore` 内存数据仓库，业务数据来自 MySQL。
- 数据库目录保留项目脚本：`schema.sql`、`seed.sql`。
- 接口契约覆盖登录、房态、预订、入住、退房、账单、清洁、客史、报表。
- 测试数据包含账号、98 间房、预订、在住、账单、清洁任务。
- `mvn test` 已通过：6 个测试，0 失败，0 错误。
- 已用 `java -jar target/hrms-backend-0.1.0.jar` 启动验证。
- 已验证 `/api/auth/test-accounts`、登录、`/api/rooms` 返回 98 间房。
- 已验证新增预订、取消预订、Walk-in 入住、消费入账、退房生成清洁任务、清洁完成、报表查询。
