from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
TEST_RUN_ID = "019f21ac-5bd7-70f0-ada5-30be4ba097be"
AUTHOR = "XXX"
DATE = "2026-07-02"


def write(name: str, body: str) -> None:
    path = ROOT / name
    path.write_text(body.strip() + "\n", encoding="utf-8")


common_header = f"""\
> 项目：酒店管理系统
> 负责人：{AUTHOR}
> 角色：测试工程师 / 文档助理
> 日期：{DATE}
> 测试记录编号：{TEST_RUN_ID}
"""


docs = {
    "README.md": f"""\
# 酒店管理系统交付材料

{common_header}

本目录用于课程提交和答辩审核，集中保存测试工程师与文档助理负责的材料。Markdown 文件是可追溯源文件，`酒店管理系统_全套软件工程文档.docx` 是统一排版后的 Word 成品，`酒店管理系统_答辩PPT初稿.pptx` 是答辩 PPT 初稿。

## 目录说明

| 文件 | 用途 |
| --- | --- |
| `01-需求规格说明书.md` | 系统背景、用户角色、功能需求和非功能需求 |
| `02-概要设计说明书.md` | 总体架构、模块划分、核心流程和部署视图 |
| `03-详细设计说明书.md` | 后端分层、前端页面、接口调用和异常处理细节 |
| `04-数据库设计说明书.md` | 核心表、字段职责、测试数据和约束说明 |
| `05-接口设计说明书.md` | 统一响应、鉴权、主要 API 和数据约定 |
| `06-用户手册.md` | 面向演示和验收的功能操作说明 |
| `07-部署运行手册.md` | 后端、数据库、前端和常见问题处理 |
| `08-软件测试报告.md` | 测试范围、执行记录、缺陷风险和补测建议 |
| `09-答辩材料清单.md` | 答辩 PPT、演示、文档和提交材料检查 |
| `10-演示视频脚本和录制清单.md` | 演示视频镜头顺序、旁白稿和录制检查项 |
| `11-格式排版查重自查表.md` | 统一格式、排版、原创性和引用自查 |

## 交付原则

- 文档统一使用 UTF-8 编码、Markdown 源文件和 Word 成品双轨维护。
- 所有结论来自本地仓库、运行测试和课程岗位文档，不编造未验证功能。
- 查重部分只做本地原创性和格式自查，不声称已通过外部查重平台。
- 答辩 PPT 初稿需要全组一起审核，本目录仅提供初稿和录制脚本。

## 外部结构参考

- Jama Software: https://www.jamasoftware.com/requirements-management-guide/writing-requirements/software-design-specification/
- Rutgers Software Engineering Project Report: https://eceweb1.rutgers.edu/~marsic/Teaching/SE/report1.html
""",
    "01-需求规格说明书.md": f"""\
# 01 需求规格说明书

{common_header}

## 1. 引言

酒店管理系统面向课程实践中的前台、客房服务、管理和测试演示场景，目标是完成客房管理、预订、入住、退房、账单、清洁、客史和报表等核心业务闭环。系统由 Spring Boot 后端、MySQL 数据库和 uni-app 前端组成，支持网页端和 APP 端源码形态。

## 2. 用户角色

| 角色 | 主要目标 | 典型操作 |
| --- | --- | --- |
| 管理员 | 管理系统数据并查看经营状态 | 登录、查看房态、维护房间、查看报表 |
| 前台人员 | 处理预订、入住和退房 | 新增预订、办理入住、消费入账、退房结算 |
| 客房服务员 | 处理清洁任务并反馈房态 | 查看清洁任务、更新任务状态、完成检查 |
| 测试人员 | 验证业务闭环和交付质量 | 执行自动化测试、冒烟测试、缺陷记录 |

## 3. 功能需求

| 编号 | 模块 | 需求描述 | 验收要点 |
| --- | --- | --- | --- |
| FR-01 | 认证登录 | 用户可使用测试账号登录并获取 token | 返回用户身份、角色和可用于后续请求的 token |
| FR-02 | 房态管理 | 查询全部房间、按楼层/状态筛选、查看房间详情 | 房态枚举统一为 `vacant/occupied/reserved/dirty/cleaning/maintenance` |
| FR-03 | 预订管理 | 创建和取消预订，预订成功后占用对应房间 | 不允许无效日期或无可用房间的预订 |
| FR-04 | 入住登记 | 支持预订入住和 Walk-in 入住 | 生成入住记录、客史和账单，房间变为入住 |
| FR-05 | 退房结算 | 结清账单并释放入住记录 | 房间进入脏房状态并自动生成清洁任务 |
| FR-06 | 账单管理 | 查询账单、查看明细、追加服务消费 | 金额计算使用精确小数，服务费进入汇总 |
| FR-07 | 清洁管理 | 清洁任务从待处理到进行中再到完成 | 检查通过后房间恢复空净 |
| FR-08 | 客史管理 | 查询住客历史信息 | 退房和新增入住后数据可追溯 |
| FR-09 | 报表统计 | 展示入住率和营收统计 | 返回总房数、入住率、总营收和账单数 |
| FR-10 | 前端展示 | 网页端和 APP 端提供主要页面源码 | 页面路径、组件结构和图标资源完整 |

## 4. 非功能需求

| 类型 | 要求 |
| --- | --- |
| 可用性 | 后端支持本地 `java -jar` 启动，数据库脚本可重建测试数据 |
| 可维护性 | 后端采用 Controller、Service、Mapper、Entity、DTO 分层 |
| 可测试性 | 至少保留 Maven/JUnit 自动化测试和 API 冒烟测试记录 |
| 一致性 | API 返回统一响应结构，文档中的接口和枚举与代码一致 |
| 安全性 | 明确标记数据库密码、Token secret、前端地址硬编码风险 |

## 5. 范围边界

本次交付覆盖本地运行、接口验证和课程答辩所需文档。前端目录当前没有 `package.json`，因此不承诺命令行前端构建和浏览器 E2E 已完成。WebSocket 已有最小实现，但本次测试记录未覆盖真实客户端订阅验证。

## 6. 验收标准

- 后端 `mvn test` 通过，现有 6 个测试 0 失败。
- 后端可打包为 `target/hrms-backend-0.1.0.jar`。
- 数据库执行 `schema.sql` 和 `seed.sql` 后可获得 98 间房及基础业务数据。
- 核心 API 冒烟测试通过 22 项断言。
- 文档、PPT、演示脚本可以对应课程岗位要求逐项追溯。
""",
    "02-概要设计说明书.md": f"""\
# 02 概要设计说明书

{common_header}

## 1. 总体架构

系统采用前后端分离的教学实践结构。后端是单体 Spring Boot 应用，负责认证、业务规则、数据访问和接口输出；数据库使用 MySQL 保存角色、房间、预订、入住、账单、清洁和报表基础数据；前端由 uni-app 网页端和 APP 端源码组成，负责页面展示与接口调用。

## 2. 技术选型

| 层次 | 技术 | 说明 |
| --- | --- | --- |
| 后端 | Spring Boot 3.3.1 | Web、Validation、WebSocket、统一异常和鉴权 |
| 数据访问 | MyBatis | Mapper 注解和 XML 混合，实体来自 MySQL |
| 数据库 | MySQL 8.0 | `schema.sql` 建表，`seed.sql` 初始化测试数据 |
| 接口文档 | Knife4j / Swagger | `/swagger-ui.html` 和 `/doc.html` |
| 前端 | uni-app / Vue 单文件组件 | 网页端和 APP 端分别维护页面与资源 |

## 3. 后端模块划分

| 模块 | 责任 |
| --- | --- |
| `common` | 统一响应、异常处理、鉴权拦截、角色校验、业务异常 |
| `config` | Web MVC 拦截器和 WebSocket 配置 |
| `controller` | 接收 HTTP 请求，完成参数校验和响应封装 |
| `service` / `service.impl` | 业务流程编排、状态流转和金额计算 |
| `mapper` | MyBatis 数据访问接口 |
| `entity` | 数据库表实体 |
| `dto.request` / `dto.response` | 前后端传输对象 |
| `utils` | Token、金额、状态映射、ID 生成、房态推送 |

## 4. 核心业务流程

1. 登录：校验用户名和密码，生成 token，返回用户角色。
2. 新增预订：校验入住退房日期、房型和空房，创建预订并更新房态。
3. 入住：根据预订或 Walk-in 请求创建客史、入住记录和账单。
4. 消费入账：向账单追加服务消费，用 `BigDecimal` 重算金额。
5. 退房：结算账单，更新入住记录，房间转为脏房并创建清洁任务。
6. 清洁：任务状态流转，检查完成后房间恢复空净。
7. 报表：按数据库当前业务状态生成入住率和营收汇总。

## 5. 部署视图

| 节点 | 端口 | 内容 |
| --- | --- | --- |
| 后端服务 | `8080`，测试时可改 `18080` | Spring Boot API、Swagger、WebSocket |
| MySQL | `3306` | `hrms` 数据库 |
| 前端网页 | `8081`，Nginx 示例 | HBuilderX 发行后的静态资源 |

## 6. 设计约束

- 后端默认配置中包含本地数据库密码和 token secret，课程演示可用，生产环境需要环境变量或 profile 覆盖。
- 前端网页端 `utils/api.js` 当前写死 `http://localhost:8080`，部署时需要统一配置方式。
- APP 端 `pages.json` 含 JSONC 注释，HBuilderX 通常可识别，严格 JSON 校验工具会报错。
""",
    "03-详细设计说明书.md": f"""\
# 03 详细设计说明书

{common_header}

## 1. 后端分层设计

| 层次 | 代表类/目录 | 设计说明 |
| --- | --- | --- |
| Controller | `AuthController`、`RoomController`、`ReservationController` 等 | 对外暴露 REST API，调用 Service，并返回 `ApiResponse` |
| Service | `RoomService`、`CheckInService`、`BillingService` 等 | 定义业务能力和事务边界 |
| ServiceImpl | `RoomServiceImpl`、`CheckInServiceImpl` 等 | 编排 Mapper、校验业务状态、更新数据库 |
| Mapper | `RoomMapper`、`BillMapper`、`ReportMapper` 等 | 使用 MyBatis 操作表数据 |
| DTO | `dto.request`、`dto.response` | 隔离接口字段和数据库实体 |

## 2. 状态设计

| 业务对象 | 状态 | 说明 |
| --- | --- | --- |
| 房间 | `vacant` | 空净，可预订或入住 |
| 房间 | `occupied` | 已入住 |
| 房间 | `reserved` | 已预订 |
| 房间 | `dirty` | 退房后待清洁 |
| 房间 | `cleaning` | 清洁处理中 |
| 房间 | `maintenance` | 维修，不可入住 |
| 清洁任务 | `pending/doing/done` | 待处理、进行中、已完成 |
| 预订 | `pending/cancelled/checked_in` | 待入住、已取消、已入住 |

## 3. 关键设计点

- 统一响应：所有接口返回 `code/message/data`，便于前端统一处理。
- 鉴权：除登录和测试账号接口外，其余接口通过 `Authorization: Bearer <token>` 访问。
- 角色校验：需要限制权限的接口使用角色注解或拦截逻辑。
- 金额计算：账单金额使用 `BigDecimal`，避免浮点误差。
- ID 生成：业务 ID 使用前缀加日期/序列，便于测试追踪。
- WebSocket：房态变更后可通过 `/ws/room-status` 推送，当前测试未验证客户端订阅。

## 4. 前端页面设计

| 页面 | 主要职责 |
| --- | --- |
| 登录页 | 获取测试账号、登录、保存 token |
| 首页/仪表盘 | 展示入住率、营收、房态摘要 |
| 房态管理 | 房间列表、状态筛选、房态修改 |
| 预订管理 | 新增预订、取消预订、查看预订 |
| 入住登记 | 预订入住和 Walk-in 入住 |
| 退房结算 | 查询账单并完成退房 |
| 账单管理 | 账单列表、明细、消费入账 |
| 清洁管理 | 清洁任务状态更新 |
| 客史/报表 | 查询客户历史和统计结果 |

## 5. 异常处理设计

| 类型 | 处理方式 |
| --- | --- |
| 参数错误 | 返回 `400` 业务码和参数提示 |
| 未登录/Token 失效 | 返回 `401` 业务码 |
| 无权限 | 返回 `403` 业务码 |
| 业务冲突 | 返回 `409` 业务码，例如房间不可入住 |
| 服务异常 | 返回 `500` 业务码，建议后续补充稳定非空错误消息 |

## 6. 可改进项

- 数据库凭据和 token secret 应从环境变量读取。
- 前端 API 地址应支持构建环境或运行环境配置。
- WebSocket 客户端订阅和浏览器 E2E 测试需要补测。
""",
    "04-数据库设计说明书.md": f"""\
# 04 数据库设计说明书

{common_header}

## 1. 数据库概况

数据库名为 `hrms`，由 `后端/hrms-backend/src/main/resources/db/schema.sql` 创建结构，并由 `seed.sql` 初始化演示与测试数据。测试完成后已重新导入脚本，将本机数据恢复到种子状态。

## 2. 核心表

| 表名 | 职责 | 关键数据 |
| --- | --- | --- |
| `sys_user` | 系统用户和登录账号 | 初始化 5 个测试账号 |
| `sys_role` | 角色信息 | 管理员、前台、客房等角色 |
| `room_type` | 房型定义 | 房型编号、名称、价格 |
| `room` | 房间和房态 | 初始化 98 间房 |
| `reservation` | 预订记录 | 初始化 2 条预订 |
| `guest` | 客户档案 | 住客基础信息 |
| `check_in_record` | 入住记录 | 入住、退房和房间占用记录 |
| `bill` | 账单主表 | 初始化 2 条账单 |
| `bill_item` | 账单明细 | 房费、服务费等 |
| `housekeeping_task` | 清洁任务 | 初始化 2 条任务 |

## 3. 数据关系

- 一个房型可对应多间房间。
- 一条预订关联房型、房间和住客信息。
- 一次入住生成一条入住记录、一个住客档案和一张账单。
- 一张账单包含多条账单明细。
- 退房后自动生成清洁任务，清洁完成后更新房态。

## 4. 测试数据量

| 表 | 记录数 |
| --- | --- |
| `sys_user` | 5 |
| `room` | 98 |
| `reservation` | 2 |
| `bill` | 2 |
| `housekeeping_task` | 2 |

## 5. 设计注意事项

- 金额字段应使用精确小数类型，并在 Java 侧使用 `BigDecimal`。
- 房态、预订状态、清洁状态需要保持枚举一致。
- 测试前后建议重新导入 `schema.sql` 和 `seed.sql`，避免冒烟测试修改影响后续演示。
""",
    "05-接口设计说明书.md": f"""\
# 05 接口设计说明书

{common_header}

## 1. 统一响应

```json
{{
  "code": 0,
  "message": "success",
  "data": {{}}
}}
```

| 错误码 | 含义 |
| --- | --- |
| `400` | 参数错误 |
| `401` | 未登录或 token 失效 |
| `403` | 无权限 |
| `404` | 资源不存在 |
| `409` | 业务冲突 |
| `500` | 服务异常 |

## 2. 鉴权方式

- 登录接口：`POST /api/auth/login`
- 请求头：`Authorization: Bearer <token>`
- 公开接口：`GET /api/auth/test-accounts`

## 3. 主要接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/api/auth/login` | 登录 |
| `GET` | `/api/auth/test-accounts` | 测试账号 |
| `GET` | `/api/rooms` | 房态列表，支持 `status/floor` |
| `GET` | `/api/rooms/{{number}}` | 房间详情 |
| `PATCH` | `/api/rooms/{{number}}/status` | 修改房态 |
| `POST` | `/api/rooms/{{number}}/change-room` | 换房 |
| `PATCH` | `/api/rooms/{{number}}/extend-stay` | 续住 |
| `GET` | `/api/reservations` | 预订列表 |
| `POST` | `/api/reservations` | 新增预订 |
| `POST` | `/api/reservations/{{id}}/cancel` | 取消预订 |
| `POST` | `/api/checkin` | 预订入住或 Walk-in 入住 |
| `POST` | `/api/checkout` | 退房结算 |
| `GET` | `/api/bills` | 账单列表 |
| `GET` | `/api/bills/{{id}}` | 账单详情 |
| `POST` | `/api/bills/{{id}}/charges` | 消费入账 |
| `GET` | `/api/housekeeping/tasks` | 清洁任务列表 |
| `PATCH` | `/api/housekeeping/tasks/{{id}}` | 更新清洁任务 |
| `GET` | `/api/guests` | 客史列表 |
| `GET` | `/api/reports/occupancy` | 入住率报表 |
| `GET` | `/api/reports/revenue` | 营收报表 |

## 4. 关键字段约定

| 字段 | 说明 |
| --- | --- |
| `roomNumber` / `number` | 房间号 |
| `status` | 前端房态枚举 |
| `type` / `roomType` | 房型编号 |
| `checkin` / `checkout` | 入住和退房日期 |
| `totalAmount` / `serviceAmount` | 账单金额 |

## 5. 联调约定

- 前端保存 token 后统一附加到后续 API 请求。
- 后端返回业务错误时，前端展示 `message`。
- 当前网页端 API 基址为 `http://localhost:8080`，如果后端使用 `18080` 测试端口，需要同步调整或通过代理转发。
""",
    "06-用户手册.md": f"""\
# 06 用户手册

{common_header}

## 1. 登录系统

1. 打开前端页面或 Swagger 接口页面。
2. 使用测试账号接口查看可用账号。
3. 输入用户名和密码登录，例如 `admin / 123456`。
4. 登录成功后进入首页或获取接口 token。

## 2. 查看房态

- 进入房态管理页面。
- 可按楼层或房态筛选房间。
- 房态颜色和状态文本应与后端返回一致。
- 可对可操作房间执行维修、恢复空净等状态变更。

## 3. 办理预订

1. 进入预订管理页面。
2. 填写姓名、手机号、房型、入住日期和退房日期。
3. 提交预订。
4. 系统返回预订记录，目标房间状态变为已预订。

## 4. 办理入住

- 预订入住：选择已有预订并确认入住。
- Walk-in 入住：直接选择可用房型或房间，录入住客信息。
- 入住成功后系统创建客史、入住记录和账单。

## 5. 消费入账和退房

1. 在账单管理中选择目标账单。
2. 如有额外服务消费，追加费用项目。
3. 进入退房结算，确认应收金额和付款状态。
4. 退房成功后房间进入脏房状态，并生成清洁任务。

## 6. 客房清洁

- 客房服务员查看待处理任务。
- 将任务从待处理更新为进行中。
- 完成清洁并通过检查后，房态恢复为空净。

## 7. 报表查看

- 入住率报表展示总房数、在住数和入住率。
- 营收报表展示总营收、服务收入和账单数。
- 报表数据来自当前数据库状态。

## 8. 常见问题

| 问题 | 处理方式 |
| --- | --- |
| 登录失败 | 确认已导入 `seed.sql`，并使用测试账号 |
| 接口返回数据库错误 | 确认 MySQL 启动、密码正确、数据库已初始化 |
| 前端访问不到后端 | 检查 `BASE_URL` 和后端端口是否一致 |
| APP 端配置 JSON 校验失败 | 使用 HBuilderX 或 JSONC 解析；严格 JSON 工具需移除注释 |
""",
    "07-部署运行手册.md": f"""\
# 07 部署运行手册

{common_header}

## 1. 环境要求

| 工具 | 版本建议 |
| --- | --- |
| JDK | 17 或更高 |
| Maven | 3.6 或更高 |
| MySQL | 8.0 |
| 前端工具 | HBuilderX / uni-app 相关环境 |

## 2. 初始化数据库

在 MySQL 客户端中按顺序执行：

```sql
source 后端/hrms-backend/src/main/resources/db/schema.sql;
source 后端/hrms-backend/src/main/resources/db/seed.sql;
```

如果使用图形化工具，先执行 `schema.sql`，再执行 `seed.sql`。

## 3. 后端配置

配置文件位置：

```text
后端/hrms-backend/src/main/resources/application.yml
```

默认端口为 `8080`。如果端口被占用，可启动时追加：

```powershell
--server.port=18080
```

如果本机 MySQL 密码不同，可启动时追加：

```powershell
--spring.datasource.password=<本机有效密码>
```

## 4. 后端启动

```powershell
cd "后端/hrms-backend"
mvn test
mvn package -DskipTests
java -Dfile.encoding=UTF-8 -jar "target/hrms-backend-0.1.0.jar"
```

## 5. 接口文档

- Swagger UI：`http://localhost:8080/swagger-ui.html`
- Knife4j：`http://localhost:8080/doc.html`
- 测试账号：`http://localhost:8080/api/auth/test-accounts`
- HTTP 样例：`后端/http/hrms-api.http`

## 6. 前端运行

网页端和 APP 端均为 HBuilderX/uni-app 工程。网页端可使用 HBuilderX 执行发行流程，生成静态资源后放入 Nginx 目录，参考 `后端/nginx/hrms-web.conf`。

## 7. 部署风险

- 默认数据库密码不一定适配所有机器。
- 前端网页端 API 地址当前写死为 `http://localhost:8080`。
- 当前前端目录缺少 CLI 构建入口，CI 构建需要后续补齐 `package.json`。
""",
    "08-软件测试报告.md": f"""\
# 08 软件测试报告

{common_header}

## 1. 测试概况

| 项目 | 内容 |
| --- | --- |
| 被测仓库 | `Agent2026-Li/Hotel-management-system` |
| 测试记录编号 | `{TEST_RUN_ID}` |
| 本地基线提交 | `b7ef6bb5ac756ab3b95461ccfb45c9caef7388ec` |
| 测试报告提交 | `b5e1452ed4df633a6365b4e934f3b05730069201` |
| 测试时间 | 2026-07-02 15:00-15:46, Asia/Shanghai |
| 测试环境 | Windows 11, Java 26, Apache Maven 3.9.16, MySQL80 |

## 2. 测试范围

| 范围 | 结果 |
| --- | --- |
| 后端 Maven/JUnit 测试 | 6 个测试通过，0 失败 |
| 后端打包 | `mvn package -DskipTests` 成功 |
| 后端运行时冒烟 | `java -jar` 启动成功，测试端口 `18080` |
| 数据库初始化 | `schema.sql`、`seed.sql` 成功导入 |
| 核心 API 冒烟 | 22 项断言通过 |
| 前端静态检查 | 网页端通过，APP 端 JSONC 兼容通过 |

## 3. 执行结果

| 模块 | 用例摘要 | 结果 |
| --- | --- | --- |
| 认证 | 获取测试账号、`admin/123456` 登录、无 token 拦截 | 通过 |
| 房态 | 查询 98 间房、一楼 14 间、修改并恢复 `106` 状态 | 通过 |
| 预订 | 查询 2 条种子预订、新增并取消预订 | 通过 |
| 入住 | 使用种子预订办理 `102` 入住 | 通过 |
| 账单 | 入住后账单增加，`B001` 追加服务费 25 | 通过 |
| 退房 | 退房后生成 `102` 清洁任务 | 通过 |
| 清洁 | 任务从 `pending` 到 `doing` 到 `done` | 通过 |
| 客史 | 客史列表包含新增入住客人 | 通过 |
| 报表 | 入住率 2.04，总营收 1080，服务收入 25 | 通过 |

## 4. 缺陷与风险

| 编号 | 级别 | 类型 | 说明 | 建议 |
| --- | --- | --- | --- | --- |
| D-001 | 中 | 配置风险 | 默认数据库密码在当前机器不可用 | 使用 profile 或环境变量覆盖 |
| R-001 | 中 | 安全风险 | 数据库密码和 token secret 明文在仓库中 | 改为环境变量 |
| R-002 | 中 | 部署风险 | 前端 API 地址硬编码 | 使用代理或条件编译 |
| R-003 | 中 | 可维护性 | 数据库异常返回 `message=null` | 全局异常处理返回稳定错误消息 |
| R-004 | 中 | 工程化 | 前端缺少 CLI 构建入口 | 补充 `package.json` 和构建命令 |
| R-005 | 低 | 质量 | 前端保留调试日志 | 仅开发环境输出 |

## 5. 未覆盖内容

- 前端真实构建和浏览器 E2E。
- 移动端真机或模拟器测试。
- WebSocket 客户端订阅验证。
- 并发预订、重复入住、欠费退房等边界业务测试。

## 6. 结论

后端核心功能冒烟通过，前端静态检查基本通过，数据库可重建测试数据。系统已达到课程演示和答辩的基础验收条件，但后续若进入更完整验收，需要补齐前端工程化构建、E2E 测试、WebSocket 订阅测试和配置外置化。
""",
    "09-答辩材料清单.md": f"""\
# 09 答辩材料清单

{common_header}

## 1. 答辩目标

答辩需要说明系统为什么做、做了哪些功能、采用什么架构、如何联调和测试、当前有哪些风险与后续改进。PPT 初稿由文档助理负责制作，全组审核后定稿。

## 2. PPT 初稿结构

| 页码 | 标题 | 讲解重点 |
| --- | --- | --- |
| 1 | 酒店管理系统完成了核心业务闭环 | 项目目标、技术栈、角色分工 |
| 2 | 答辩从业务闭环展开 | 讲解路线 |
| 3 | 需求覆盖前台、客房和管理三类场景 | 用户角色和功能边界 |
| 4 | 系统采用前后端分离和后端分层设计 | 架构、模块、数据流 |
| 5 | 预订到清洁形成完整状态流转 | 核心业务流程 |
| 6 | 后端以 MyBatis 和 MySQL 承载真实数据 | 数据库、Mapper、接口 |
| 7 | 前端提供网页端和 APP 端页面源码 | 页面和交互 |
| 8 | 测试结果证明后端核心链路可运行 | 6 个单测、22 项冒烟 |
| 9 | 当前风险集中在配置和工程化 | 缺陷、风险、改进 |
| 10 | 演示按登录、房态、入住、退房、报表收束 | 演示顺序和总结 |

## 3. 提交材料

| 材料 | 状态 |
| --- | --- |
| 源代码仓库 | 已推送测试报告提交 |
| 软件测试报告 | 已整理 |
| 全套软件工程文档 Markdown | 已整理 |
| Word 成品 | 已生成并待最终组内确认 |
| 答辩 PPT 初稿 | 已生成并待全组审核 |
| 演示视频脚本 | 已生成 |
| 格式排版查重自查表 | 已生成 |

## 4. 组内审核建议

- 后端同学确认接口、数据库和部署说明是否与当前代码一致。
- 前端同学确认页面名称、页面路径和演示流程是否可展示。
- 测试同学确认测试结论、缺陷风险和未覆盖项没有夸大。
- 组长确认 PPT 时长、每人讲解页和最终提交命名。
""",
    "10-演示视频脚本和录制清单.md": f"""\
# 10 演示视频脚本和录制清单

{common_header}

## 1. 录制前检查

| 检查项 | 标准 |
| --- | --- |
| 数据库 | 已执行 `schema.sql` 和 `seed.sql` |
| 后端 | `java -jar` 启动成功，Swagger 可访问 |
| 端口 | 后端端口与前端 `BASE_URL` 一致 |
| 账号 | `admin / 123456` 可登录 |
| 屏幕 | 浏览器缩放 100%，录屏区域只保留演示窗口 |
| 数据 | 使用种子数据演示，避免录入真实个人信息 |

## 2. 镜头顺序

| 镜头 | 画面 | 操作 | 旁白要点 |
| --- | --- | --- | --- |
| 1 | 项目目录和 README | 展示仓库结构 | 本项目包含后端、网页端、APP 端和交付文档 |
| 2 | Swagger 或登录页 | 获取测试账号并登录 | 系统使用 token 鉴权，测试账号来自种子数据 |
| 3 | 房态页面 | 筛选房间、查看房态 | 房态是预订、入住和清洁流程的核心 |
| 4 | 预订页面 | 新增或展示预订 | 预订会占用可用房间 |
| 5 | 入住页面 | 办理入住 | 入住生成客史、入住记录和账单 |
| 6 | 账单页面 | 查看账单、追加消费 | 账单支持房费和服务费 |
| 7 | 退房页面 | 完成退房结算 | 退房后自动生成清洁任务 |
| 8 | 清洁页面 | 更新清洁任务 | 清洁完成后房态恢复空净 |
| 9 | 报表页面 | 查看入住率和营收 | 报表来自当前数据库状态 |
| 10 | 测试报告 | 展示 22 项冒烟通过 | 说明已完成核心链路验证和风险记录 |

## 3. 推荐旁白稿

大家好，我们展示的是酒店管理系统。本系统围绕酒店客房业务闭环设计，覆盖登录、房态、预订、入住、退房、账单、清洁、客史和报表。后端采用 Spring Boot、MyBatis 和 MySQL，前端提供网页端和 APP 端源码。演示中我们使用初始化脚本提供的测试账号和种子数据，先登录系统，再按业务流程展示从房间查询到预订入住、退房清洁和报表统计的完整链路。最后展示测试报告，本次后端自动化测试通过，核心 API 冒烟 22 项断言通过，同时记录了配置外置化、前端 CLI 构建入口和 WebSocket 补测等后续风险。

## 4. 录制后检查

- 视频中没有暴露真实密码或个人隐私。
- 页面文字清晰，接口响应没有长时间卡顿。
- 讲解覆盖项目目标、核心功能、技术实现、测试结果和风险。
- 文件命名建议：`酒店管理系统_演示视频_XXX.mp4`。
""",
    "11-格式排版查重自查表.md": f"""\
# 11 格式排版查重自查表

{common_header}

## 1. 格式自查

| 检查项 | 标准 | 状态 |
| --- | --- | --- |
| 文件命名 | 使用编号和中文标题，便于排序 | 已检查 |
| 编码 | Markdown 使用 UTF-8 | 已检查 |
| 标题层级 | 一级标题为文档名，二级标题为章节 | 已检查 |
| 表格 | 表头完整，列含义清楚 | 已检查 |
| 代码和路径 | 使用反引号标注 | 已检查 |
| 日期和署名 | 日期统一为 2026-07-02，负责人暂写 XXX | 已检查 |
| 风险表述 | 区分已验证结论和未覆盖内容 | 已检查 |

## 2. 排版自查

| 检查项 | 标准 | 状态 |
| --- | --- | --- |
| Word 样式 | 使用统一标题、正文和表格样式 | 已生成 |
| PPT 样式 | 使用单色网格，标题和正文不拥挤 | 已生成 |
| 表格宽度 | Word 表格不超出页面 | 待渲染确认 |
| 页眉页脚 | Word 成品包含项目名和日期 | 已生成 |
| PPT 页码 | 每页有页码或清晰结构 | 已生成 |

## 3. 原创性自查

| 检查项 | 说明 | 状态 |
| --- | --- | --- |
| 内容来源 | 主要来自本地代码、测试记录和课程岗位文档 | 已检查 |
| 外部模板 | 只参考软件工程文档结构，不复制正文 | 已检查 |
| 引用说明 | 参考链接已放入 README | 已检查 |
| 查重声明 | 未使用外部查重平台，不声明查重通过 | 已明确 |

## 4. 岗位要求覆盖

| 岗位要求 | 对应材料 |
| --- | --- |
| 编写测试计划、测试用例、功能测试 | `08-软件测试报告.md` |
| 提交缺陷清单，跟踪修复 | `08-软件测试报告.md` 第 4 节 |
| 撰写软件测试报告 | `测试文档.md` 和 `08-软件测试报告.md` |
| 整理概要设计、详细设计、用户手册等 | `01` 到 `07` 号文档 |
| 制作答辩 PPT 初稿 | `酒店管理系统_答辩PPT初稿.pptx` |
| 录演示视频、整理答辩材料 | `09` 和 `10` 号文档 |
| 统一文档格式、排版、查重 | 本自查表和 Word 成品 |
""",
}


def main() -> None:
    ROOT.mkdir(parents=True, exist_ok=True)
    for filename, body in docs.items():
        write(filename, body)
    print(f"generated {len(docs)} markdown files in {ROOT}")


if __name__ == "__main__":
    main()
